ls# CISC275-Fall2018-first-git
1. Create java files to make this code compile and run.

2. What five objects are created in the main?
    1 comparator, 1 arraylist, and 3 Dogs

3. Can you spot the comparator constructor call? Where is the class definition for the comparator?

Its inside the sort function. The compare method was written inside of the new comparator declared new inside collections.sort. Method-ception